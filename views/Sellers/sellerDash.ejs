<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Seller Dashboard | Shopsy</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</head>
<body class="bg-gradient-to-br from-gray-100 to-white min-h-screen font-sans">

  <!-- 🧭 Navbar -->
  <nav class="bg-white max-w-7xl mx-auto shadow-md px-6 py-4 flex justify-between items-center sticky top-0 z-50">
    <h1 class="text-2xl font-extrabold text-indigo-600 tracking-wide">🛒 Shopsy Seller Dashboard</h1>
    <a href="/logout" class="bg-red-500 hover:bg-red-600 text-white font-semibold px-4 py-2 rounded-full shadow-sm transition-all duration-200">
      <i class="fas fa-sign-out-alt mr-1"></i> Logout
    </a>
  </nav>

  <!-- 🔧 Main Layout -->
  <main class="max-w-7xl mx-auto px-4 py-8 grid grid-cols-1 md:grid-cols-4 gap-6">

   <!-- 📋 Left Sidebar (Quick Links) -->
<aside class="hidden md:flex flex-col bg-white p-6 rounded-2xl shadow-lg w-full h-[100vh] sticky top-20">
  <h2 class="text-xl font-bold text-gray-800 mb-6">📂 Quick Links</h2>
  <nav class="flex flex-col gap-3">
    <a href="/" class="flex items-center gap-3 px-4 py-3 bg-indigo-50 hover:bg-indigo-100 text-indigo-600 rounded-lg transition-all duration-200 shadow-sm">
      <i class="fas fa-plus-circle text-lg"></i>
      <span class="text-base font-medium">Home</span>
    </a>
    
    <a href="/seller/products/list" class="flex items-center gap-3 px-4 py-3 bg-green-50 hover:bg-green-100 text-green-600 rounded-lg transition-all duration-200 shadow-sm">
      <i class="fas fa-box text-lg"></i>
      <span class="text-base font-medium">My Products</span>
    </a>
    
    <a href="/seller/orders" class="flex items-center gap-3 px-4 py-3 bg-yellow-50 hover:bg-yellow-100 text-yellow-600 rounded-lg transition-all duration-200 shadow-sm">
      <i class="fas fa-clipboard-list text-lg"></i>
      <span class="text-base font-medium">My Orders</span>
    </a>
    
    <a href="/seller/profile" class="flex items-center gap-3 px-4 py-3 bg-gray-50 hover:bg-gray-100 text-gray-700 rounded-lg transition-all duration-200 shadow-sm">
      <i class="fas fa-user-cog text-lg text-indigo-500"></i>
      <span class="text-base font-medium">Profile Settings</span>
    </a>
  </nav>
</aside>

    <!-- 🧾 Center Dashboard Area -->
    <section class="md:col-span-2">
      <div class="mb-6">
        <h2 class="text-4xl font-bold text-gray-800 mb-1">👋 Welcome, <%= user.name %>!</h2>
        <p class="text-gray-600 text-base">Manage products, track orders, and grow your business from here.</p>
      </div>

      <!-- Dashboard Cards -->
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 mt-6">
        <!-- Add Product -->
        <a href="/seller/products" class="bg-indigo-600 hover:bg-indigo-700 text-white p-6 rounded-2xl shadow-md flex flex-col items-center justify-center text-center transition transform hover:scale-105">
          <i class="fas fa-plus-circle text-3xl mb-2"></i>
          <span class="text-base font-semibold">Add Product</span>
        </a>

        <!-- My Products -->
        <a href="/seller/products/list" class="bg-green-500 hover:bg-green-600 text-white p-6 rounded-2xl shadow-md flex flex-col items-center justify-center text-center transition transform hover:scale-105">
          <i class="fas fa-box text-3xl mb-2"></i>
          <span class="text-base font-semibold">My Products</span>
        </a>

        <!-- My Orders -->
        <a href="/seller/orders" class="bg-yellow-500 hover:bg-yellow-600 text-white p-6 rounded-2xl shadow-md flex flex-col items-center justify-center text-center transition transform hover:scale-105">
          <i class="fas fa-clipboard-list text-3xl mb-2"></i>
          <span class="text-base font-semibold">My Orders</span>
        </a>
      </div>
    </section>

    <!-- 👤 Right Profile Panel (Hidden on Mobile) -->
    <aside class="hidden md:flex flex-col items-center text-center bg-white rounded-2xl shadow-lg p-6 border border-gray-100 h-[100vh] sticky top-20">
      <div class="relative">
        <img src="/productimage/<%= user.pimage %>" alt="Seller Profile"
          class="w-28 h-28 rounded-full border-4 border-indigo-600 shadow-lg object-cover mb-3 transition-transform duration-300 hover:scale-105" />
        <span class="absolute bottom-2 right-2 bg-green-500 h-4 w-4 rounded-full border-2 border-white"></span>
      </div>

      <h3 class="text-xl font-semibold text-gray-800"><%= user.name %></h3>
      <p class="text-sm text-gray-500 mb-1"><i class="fas fa-envelope mr-1 text-indigo-400"></i><%= user.email %></p>
      <span class="text-xs bg-indigo-100 text-indigo-700 px-3 py-1 rounded-full font-medium mb-3">
        🛍️ <%= user.usertype.charAt(0).toUpperCase() + user.usertype.slice(1) %>
      </span>

      <div class="w-full mt-4 text-sm text-left text-gray-600 space-y-1">
        <p><strong>Account ID:</strong> <%= user._id.toString().slice(-6).toUpperCase() %></p>
        <p><strong>Status:</strong> <span class="text-green-600 font-medium">Verified ✅</span></p>
        <p><strong>Joined:</strong> <%= new Date(user.createdAt).toLocaleDateString() %></p>
      </div>

      <a href="/seller/profile" class="mt-6 inline-block text-indigo-600 font-semibold hover:underline text-sm">
        <i class="fas fa-user-cog mr-1"></i> Manage Profile
      </a>
    </aside>

  </main>
</body>
</html>
