<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= product.pname %> | Shopsy</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900 font-sans">
  <div class="max-w-4xl mx-auto p-8 bg-white mt-6 rounded-xl shadow-md">
    <div class="flex gap-8">
      <img src="/productimage/<%= product.pimage %>" alt="<%= product.pname %>" class="w-64 h-64 object-cover rounded-lg">
      <div>
        <h1 class="text-3xl font-bold text-pink-600 mb-4"><%= product.pname %></h1>
        <p class="text-lg text-gray-700 mb-2">Price: ₹<%= product.pnewprice %></p>
        <p class="text-gray-600 mb-4"><%= product.pdescription %></p>
        <a href="/myorder" class="text-blue-600 underline">← Back to My Orders</a>
      </div>
    </div>
  </div>
</body>
</html>
