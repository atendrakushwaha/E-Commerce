<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>User Profile</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-6">

  <div class="bg-white max-w-md w-full rounded-xl shadow-lg p-8">
    <!-- Profile Header -->
    <div class="text-center mb-6">
      <img src="/productimage/<%= user.pimage %>" alt="Profile Picture"
        class="w-28 h-28 rounded-full mx-auto border-4 border-blue-500 object-cover mb-4">
      <h2 class="text-2xl font-semibold text-gray-800"><%= user.name %></h2>
    </div>

    <!-- Profile Info -->
    <div class="text-gray-700 space-y-2">
      <p><span class="font-medium w-28 inline-block text-gray-600">Email:</span> <%= user.email %></p>
      <p><span class="font-medium w-28 inline-block text-gray-600">Role:</span> <%= user.usertype %></p>
    </div>

    <!-- Edit Button -->
    <div class="text-center mt-6">
      <a href="#"
        class="inline-block bg-blue-600 text-white px-5 py-2 rounded-md text-sm hover:bg-blue-700 transition">
        Edit Profile
      </a>
    </div>
  </div>

</body>
</html>
